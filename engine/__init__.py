"""Store master normalizer engine."""

# Single source of truth for the application version, using semantic
# versioning (MAJOR.MINOR.PATCH).  Update this one line when releasing;
# see CHANGELOG.md and the "Versioning" section of README.md.
#
# NOTE: the inline ``v0.x.y`` notes scattered through the engine source are
# historical change annotations marking when a particular fix landed -- they
# are NOT the current version.  This constant is the only authoritative one.
__version__ = "1.0.2"
